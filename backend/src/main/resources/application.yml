spring:
  application:
    name: medmanage-backend
  datasource:
    url: jdbc:mysql://sql.wsfdb.cn:3306/information_schema?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: YukiRinLLflarum
    password: LUOCANYU
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  config: classpath:log4j2.xml

jwt:
  secret: medmanage_secret_key
  expiration: 86400000